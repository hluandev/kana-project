import { SidebarButton } from "./SidebarButton";

export const Sidebar = () => {
  return (
    <div className="col-span-2 border-r p-3 space-y-0.5">
      <SidebarButton text="ascii art" />
      <SidebarButton text="ascii art" />
      <SidebarButton text="ascii art" />
      <SidebarButton text="ascii art" />
      <SidebarButton text="ascii art" />
      <SidebarButton text="ascii art" />
      <SidebarButton text="ascii art" />
    </div>
  );
};
